pipeline {
    agent any
    stages {
        stage('Build Maven') {
            steps {
                echo 'Running Maven Build...'
                sh 'mvn clean install'
            }
        }
    }
}
